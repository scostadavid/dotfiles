# User definitions

# Dir
alias cd-repo='cd ~/repositories'
alias rf='rm -rf'

# Clipboard
alias clipcopy='xclip -sel c'

# adb
alias reverseall='adb start-server && adb reverse tcp:9229 tcp:9292 && adb reverse tcp:9000 tcp:9000 && adb reverse tcp:8081 tcp:8081'
